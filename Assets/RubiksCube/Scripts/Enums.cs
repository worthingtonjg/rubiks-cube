using System;

public enum EnumAxis {x, y, z};

public enum EnumDirection { positive, negative }

public enum EnumCubeAnimState { idle, animating }

public enum EnumGameState { shuffling, playing }

public enum EnumAnimType { cube, slice };

public enum EnumCubieType { core, center, edge, corner }